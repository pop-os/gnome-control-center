Index: gnome-control-center/panels/keyboard/meson.build
===================================================================
--- gnome-control-center.orig/panels/keyboard/meson.build
+++ gnome-control-center/panels/keyboard/meson.build
@@ -40,6 +40,7 @@ xml_files = [
   '01-launchers.xml',
   '01-screenshot.xml',
   '01-system.xml',
+  '10-pop-shell.xml',
   '50-accessibility.xml'
 ]
 
Index: gnome-control-center/panels/keyboard/10-pop-shell.xml.in
===================================================================
--- /dev/null
+++ gnome-control-center/panels/keyboard/10-pop-shell.xml.in
@@ -0,0 +1,28 @@
+<?xml version="1.0" encoding="UTF-8" ?>
+<KeyListEntries group="system" schema="org.gnome.shell.extensions.pop-shell" name="Pop Shell">
+    <KeyListEntry name="focus-down" description="Focus down window"/>
+    <KeyListEntry name="focus-left" description="Focus left window"/>
+    <KeyListEntry name="focus-monitor-left" description="Focus right monitor"/>
+    <KeyListEntry name="focus-monitor-right" description="Focus right monitor"/>
+    <KeyListEntry name="focus-right" description="Focus right window"/>
+    <KeyListEntry name="focus-up" description="Focus up window"/>
+    <KeyListEntry name="management-orientation" description="Toggle tiling orientation"/>
+    <KeyListEntry name="search" description="Search key combo"/>
+    <KeyListEntry name="tile-accept" description="Accept tiling changes"/>
+    <KeyListEntry name="tile-enter" description="Enter tiling mode"/>
+    <KeyListEntry name="tile-move-down" description="Move window down"/>
+    <KeyListEntry name="tile-move-left" description="Move window left"/>
+    <KeyListEntry name="tile-move-right" description="Move window right"/>
+    <KeyListEntry name="tile-move-up" description="Move window up"/>
+    <KeyListEntry name="tile-orientation" description="Toggle tiling orientation"/>
+    <KeyListEntry name="tile-reject" description="Reject tiling changes"/>
+    <KeyListEntry name="tile-resize-down" description="Resize window down"/>
+    <KeyListEntry name="tile-resize-left" description="Resize window left"/>
+    <KeyListEntry name="tile-resize-right" description="Resize window right"/>
+    <KeyListEntry name="tile-resize-up" description="Resize window up"/>
+    <KeyListEntry name="tile-swap-down" description="Swap window down"/>
+    <KeyListEntry name="tile-swap-left" description="Swap window left"/>
+    <KeyListEntry name="tile-swap-right" description="Swap window right"/>
+    <KeyListEntry name="tile-swap-up" description="Swap window up"/>
+    <KeyListEntry name="toggle-floating" description="Toggles a window between floating and tiling"/>
+</KeyListEntries>
Index: gnome-control-center/panels/keyboard/cc-keyboard-item.c
===================================================================
--- gnome-control-center.orig/panels/keyboard/cc-keyboard-item.c
+++ gnome-control-center/panels/keyboard/cc-keyboard-item.c
@@ -674,7 +674,26 @@ cc_keyboard_item_load_from_gsettings (Cc
   item->key = g_strdup (key);
   item->description = g_strdup (description);
 
-  item->settings = g_settings_new (item->schema);
+  if (g_strcmp0(schema, "org.gnome.shell.extensions.pop-shell") == 0) {
+      GSettingsSchemaSource *settings_schema_source = g_settings_schema_source_new_from_directory (
+          "/usr/share/gnome-shell/extensions/pop-shell@system76.com/schemas",
+          NULL,
+          FALSE,
+          NULL
+      );
+      GSettingsSchema *settings_schema = g_settings_schema_source_lookup (
+          settings_schema_source,
+          schema,
+          FALSE
+      );
+      item->settings = g_settings_new_full (
+          settings_schema,
+          NULL,
+          NULL
+      );
+  } else {
+      item->settings = g_settings_new (item->schema);
+  }
   g_free (item->binding);
   item->binding = settings_get_binding (item->settings, item->key);
   item->editable = g_settings_is_writable (item->settings, item->key);
@@ -881,4 +900,3 @@ cc_keyboard_item_get_settings (CcKeyboar
   g_return_val_if_fail (CC_IS_KEYBOARD_ITEM (item), NULL);
   return item->settings;
 }
-
