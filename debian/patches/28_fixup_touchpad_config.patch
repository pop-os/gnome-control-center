diff -Nur -x '*.orig' -x '*~' gnome-control-center-2.23.90/capplets/mouse/gnome-mouse-properties.c gnome-control-center-2.23.90.new/capplets/mouse/gnome-mouse-properties.c
--- gnome-control-center-2.23.90/capplets/mouse/gnome-mouse-properties.c	2008-08-31 07:43:56.000000000 +0100
+++ gnome-control-center-2.23.90.new/capplets/mouse/gnome-mouse-properties.c	2008-08-31 07:44:17.000000000 +0100
@@ -41,7 +41,9 @@
 #include <sys/types.h>
 #include <sys/stat.h>
 #include <dirent.h>
+#include <X11/Xatom.h>
 #include <X11/extensions/XInput.h>
+#include <X11/extensions/XIproto.h>
 
 #ifdef HAVE_XCURSOR
 #include <X11/Xcursor/Xcursor.h>
@@ -319,9 +321,23 @@
         int numdevices, i;      
         gboolean ret = FALSE;
         XDeviceInfo *devicelist = XListInputDevices (GDK_DISPLAY(), &numdevices);
+	XIPropertyInfo *propinfo;
+	Atom prop = XInternAtom(GDK_DISPLAY(), "Synaptics Off", False);
+
         for (i = 0; i < numdevices; i++) {
-                if (strcmp (devicelist[i].name, "Synaptics Touchpad") == 0)
-                        ret = TRUE;
+		gdk_error_trap_push();
+		XDevice *device = XOpenDevice (GDK_DISPLAY(), 
+					       devicelist[i].id);
+		if (gdk_error_trap_pop())
+			continue;
+
+		gdk_error_trap_push();
+		propinfo = XQueryDeviceProperty(GDK_DISPLAY(), device, prop);
+		XCloseDevice (GDK_DISPLAY(), device);
+		if (gdk_error_trap_pop() || (propinfo == NULL))
+			continue;
+		XFree(propinfo);
+		ret = TRUE;
         }
         
         XFreeDeviceList(devicelist);
