--- control-center-2.12.1/control-center/control-center-categories.c.orig	2005-05-02 10:05:27.000000000 +0200
+++ control-center-2.12.1/control-center/control-center-categories.c	2005-11-21 15:16:35.000000000 +0100
@@ -123,6 +123,14 @@
 	g_free (entry);
 }
 
+static int
+compare_entries (ControlCenterEntry *a,
+		    ControlCenterEntry *b)
+{
+	return strcmp (a->title, b->title);
+}
+
+
 static void
 populate_category (ControlCenterCategory *category,
 		   GMenuTreeDirectory    *menu_directory)
@@ -145,6 +153,7 @@
 		gmenu_tree_item_unref (item);
 	}
 
+	entries = g_slist_sort (entries, (GCompareFunc) compare_entries);
 	g_slist_free (items);
 
 	if (entries != NULL) {
