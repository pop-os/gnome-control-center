From: Robert Ancell <robert.ancell@canonical.com>
Date: Tue, 31 Jul 2018 04:44:11 +0100
Subject: Debian's adduser doesn't allow uppercase letters by default

Save the associated string break for Ubuntu 17.04
Forwarded: no
Bug: https://launchpad.net/bugs/1600638
---
 panels/user-accounts/um-utils.c | 1 -
 1 file changed, 1 deletion(-)

diff --git a/panels/user-accounts/um-utils.c b/panels/user-accounts/um-utils.c
index c01bc1f..d9100b9 100644
--- a/panels/user-accounts/um-utils.c
+++ b/panels/user-accounts/um-utils.c
@@ -481,7 +481,6 @@ is_valid_username (const gchar *username, gchar **tip)
                  */
                 for (c = username; *c; c++) {
                         if (! ((*c >= 'a' && *c <= 'z') ||
-                               (*c >= 'A' && *c <= 'Z') ||
                                (*c >= '0' && *c <= '9') ||
                                (*c == '_') || (*c == '.') ||
                                (*c == '-' && c != username)))
